import { ICryptoSuite, User } from 'fabric-network/lib/impl/wallet/node_modules/fabric-common';
import { Identity } from 'fabric-network/lib/impl/wallet/identity';
import { IdentityData } from 'fabric-network/lib/impl/wallet/identitydata';
import { IdentityProvider } from 'fabric-network/lib/impl/wallet/identityprovider';
export interface X509Identity extends Identity {
    type: 'X.509';
    credentials: {
        certificate: string;
        privateKey: string;
    };
}
export declare class X509Provider implements IdentityProvider {
    readonly type: string;
    private readonly cryptoSuite;
    getCryptoSuite(): ICryptoSuite;
    fromJson(data: IdentityData): X509Identity;
    toJson(identity: X509Identity): IdentityData;
    getUserContext(identity: X509Identity, name: string): Promise<User>;
}
